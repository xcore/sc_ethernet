Ethernet test suite.

Tests:

1) Tx/Rx data. Different sized packets transmitted and checked for equality
2) Tx/Rx data, check to make sure that the timing of packets is correct.
3) Check that the MAC custom filter mechanism works
4) Check that the Rx buffering will hold packets until they are requested
5) Check that Tx and Rx queueing function

